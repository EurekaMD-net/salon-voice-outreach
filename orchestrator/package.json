{
  "name": "@salon-voice-outreach/orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Voice-outreach campaign orchestrator — drives pipesong, captures every call, guards compliance. Stage 1: The Pipe.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0",
    "vlcrm": "git+https://github.com/EurekaMD-net/vlcrm.git"
  }
}
